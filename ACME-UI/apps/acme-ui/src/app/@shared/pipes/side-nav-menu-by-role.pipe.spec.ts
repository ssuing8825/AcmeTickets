import { SideNavMenuByRolePipe } from './side-nav-menu-by-role.pipe';

describe('SideNavMenuByRolePipe', () => {
  it('create an instance', () => {
    const pipe = new SideNavMenuByRolePipe();
    expect(pipe).toBeTruthy();
  });
});
