import { ReactiveFormValueToMdTableFilterPipe } from './reactive-form-value-to-md-table-filter.pipe';

describe('ReactiveFormValueToMdTableFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ReactiveFormValueToMdTableFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
