import { DynamicTemplateHostDirective } from './dynamic-template-host.directive';

describe('DynamicTemplateHostDirective', () => {
  it('should create an instance', () => {
    // const directive = new DynamicTemplateHostDirective();
    // expect(directive).toBeTruthy();
  });
});
