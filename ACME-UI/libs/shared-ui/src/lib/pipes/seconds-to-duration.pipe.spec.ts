import { SecondsToDurationPipe } from './seconds-to-duration.pipe';

describe('SecondsToDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsToDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
