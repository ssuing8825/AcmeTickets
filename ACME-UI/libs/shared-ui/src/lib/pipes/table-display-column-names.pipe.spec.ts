import { TableDisplayColumnNamesPipe } from './table-display-column-names.pipe';

describe('TableDisplayColumnNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new TableDisplayColumnNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
