import { ReadObjectByStringPathPipe } from './read-object-by-string-path.pipe';

describe('ReadObjectByStringPathPipe', () => {
  it('create an instance', () => {
    const pipe = new ReadObjectByStringPathPipe();
    expect(pipe).toBeTruthy();
  });
});
